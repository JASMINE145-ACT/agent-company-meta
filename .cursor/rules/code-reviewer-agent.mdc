---
description: Code review agent identity — Superpowers code-reviewer + platform Layer A
alwaysApply: true
---

# Code review agent (canonical)

After code changes in this repo, **code-review** means the **Superpowers `code-reviewer` subagent** — not trellis-check, not ad-hoc self-review.

## Agent identity

| Item | Value |
|------|--------|
| **Invoke** | `Task` / subagent `subagent_type: "code-reviewer"` |
| **Plugin base** | Superpowers `agents/code-reviewer.md` |
| **Base path (this machine)** | `C:\Users\m1774\.cursor\plugins\cache\cursor-public\superpowers\b7a8f76985f1e93e75dd2f2a3b424dc731bd9d37\agents\code-reviewer.md` |
| **Project extension** | `.cursor/agents/code-reviewer.md` (Layer A; same `name: code-reviewer`) |

Do **not** substitute `generalPurpose`, `trellis-check`, or inline review for this gate unless the user explicitly asks.

## Project gates

1. **Layer A** — `.trellis/spec/code-review-layer-a.md` (boundary scanners + contract wiring)
2. **Layer B** — N/A until UI renderer exists in this repo

**Order:** code-reviewer PASS → tests/smokes → docs / Trellis.

## Verdict must include

```text
Layer A: PASS | FAIL | N/A
Layer B: N/A
Runtime Crash Checklist: ...
```

Skill shortcut: `.cursor/skills/review-code/SKILL.md`
