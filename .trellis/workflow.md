# Meta Trellis — agent-company-meta

This meta repo **orchestrates** Cursor tooling and submodules.

**Product specs live in `platform/.trellis/spec/`** — do not grow WanD product specs here.

## Layout

| Path | Role |
|------|------|
| `tasks/` | Meta-level roadmap tasks only |
| `spec/` | Pointer stub → platform |
| `workspace/` | Meta session journals (optional) |
| `scripts/` | Trellis CLI (same toolkit) |

## Product work

`cd platform` and use `platform/.trellis`.
